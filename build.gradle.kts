// Root build file for AgriGo multi-module project
// This file maintains the build configuration for the entire project

plugins {
    // Plugins applied at root level
}

subprojects {
    // Common configuration for all subprojects can be added here if needed
}

